import {VuetifyOptions} from "vuetify";

const theme: VuetifyOptions['theme'] = {
    defaultTheme: 'light',
    themes:{
        light:{
            colors:{
                'primary': '#FF0000'
            }
        }
    }
}

export default theme